// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // JANGAN pakai: output: 'export'
  // biarkan default agar API Routes & Prisma jalan di server (Node.js)
};

export default nextConfig;
